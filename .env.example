# One JSON object maps stable QR slugs to changeable destinations.
QR_REDIRECTS_JSON={"futas":"https://www.facebook.com/events/REPLACE_WITH_EVENT_ID"}

# Local value used by `npm run dev:local`. Set REDIS_URL in Vercel to the
# authenticated connection string supplied by the Redis integration.
UPSTASH_REDIS_REST_REDIS_URL=redis://localhost:6379
